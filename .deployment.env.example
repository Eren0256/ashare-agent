# GitHub Container Registry credentials used only by the local deploy script.
# Create a classic PAT with read:packages. If the repository is private,
# also grant repo access.
GHCR_USERNAME=Eren0256
GHCR_TOKEN=
